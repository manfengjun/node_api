const config = {
  // 启动端口
  port: 3000,

  // 数据库配置
  mysql: {
    database: "token",
    username: "root",
    password: "jun3672830",
    port: "3306",
    host: "localhost"
  }
};

module.exports = config;
